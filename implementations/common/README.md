The following docker-compose.yml is included here only to provided a 
containerized DB for running/testing the implementations. 

To start the DB, run: 
```
docker-compose up -d
```

To tear down the DB, run: 
```
docker-compose down -v
```

